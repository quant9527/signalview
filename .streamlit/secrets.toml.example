# 复制本文件为 secrets.toml 并填入真实配置（secrets.toml 已被 .gitignore 忽略，不会提交）
# Copy this file to secrets.toml and fill in your values.

[auth]
password = "your_login_password"

[connections.quantdb]
url = "postgresql://user:password@host:5432/dbname"
